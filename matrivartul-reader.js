(()=>{const dialog=document.querySelector('#readerDialog');if(!dialog)return;const image=dialog.querySelector('#readerImage');const caption=dialog.querySelector('#readerCaption');const pages=[...document.querySelectorAll('[data-reader-page]')];let index=0;function show(i){index=(i+pages.length)%pages.length;const button=pages[index];image.src=button.dataset.full;image.alt=button.querySelector('img')?.alt||'';caption.textContent=button.querySelector('figcaption')?.textContent||''}function open(i){show(i);if(typeof dialog.showModal==='function')dialog.showModal();else dialog.setAttribute('open','')}pages.forEach((button,i)=>button.addEventListener('click',()=>open(i)));dialog.querySelector('[data-close]').addEventListener('click',()=>dialog.close());dialog.querySelector('[data-prev]').addEventListener('click',()=>show(index-1));dialog.querySelector('[data-next]').addEventListener('click',()=>show(index+1));dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});document.addEventListener('keydown',e=>{if(!dialog.open)return;if(e.key==='ArrowLeft')show(index-1);if(e.key==='ArrowRight')show(index+1);if(e.key==='Escape')dialog.close()})})();